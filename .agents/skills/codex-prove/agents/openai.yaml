interface:
  display_name: "Codex PROVE (compatibility)"
  short_description: "Temporary alias that redirects to Codex AIR."
  default_prompt: "Use legacy $codex-prove and redirect this task through the canonical $codex-air workflow."
policy:
  allow_implicit_invocation: false
